server.port: 8888

spring:
   application:
     name: configserver
   profiles:
      active: native
   cloud:
      config:
         server:
            native:
               searchLocations: file:///c:/code/spring-cloud-introduction/config-yml