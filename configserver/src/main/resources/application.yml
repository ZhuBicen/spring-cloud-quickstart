server.port: 8888

spring:
  output:
    ansi:
      enabled: always
  application:
      name: configserver
  profiles:
     active: native
  cloud:
     config:
       server:
         native:
           searchLocations: file:///d:/Code/spring-cloud-quickstart/config-yml